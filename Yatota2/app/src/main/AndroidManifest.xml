<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mynet.yatota">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LiveShopActivity"></activity>
        <activity android:name=".Activities.MyCartActivity" />
        <activity android:name=".Activities.ProOrderActivity" />
        <activity android:name=".Activities.Amazondata" />
        <activity android:name=".Activities.Imeldadata" />
        <activity android:name=".Activities.Cosmetologist1" />
        <activity android:name=".Activities.ShopData" />
        <activity android:name=".Activities.InfoAnalysis" />
        <activity android:name=".Activities.ManagerPortal" />
        <activity android:name=".Activities.ProductDetails" />
        <activity android:name=".Activities.ProductHome" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>